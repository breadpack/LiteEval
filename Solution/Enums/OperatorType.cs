namespace LiteEval.Enums {
    public enum OperatorType : byte {
        Add,
        Subtract,
        Multiply,
        Divide,
        Power,
        ParenthesisStart,
        ParenthesisEnd,
        UnaryMinus,
        Comma,
    }
}